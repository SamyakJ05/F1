import { AtlasShell } from "../components/AtlasShell";
import { CircuitMap } from "../components/CircuitMap";
import { calendar2026, calendar2026SourceUrl, circuits } from "../site-data";

export default function CircuitsPage() {
  return <AtlasShell><section className="atlas-hero"><p className="section-kicker"><span>01</span> CIRCUIT ATLAS</p><h1>Read the track<br /><em>before the apex.</em></h1><p>Three real-geometry circuit notes, plus every venue on the current 2026 World Championship calendar.</p></section><section className="atlas-grid-section"><div className="circuit-library">{circuits.map((circuit, index) => <article className="circuit-library-card" key={circuit.slug}><div className="circuit-card-art"><CircuitMap slug={circuit.slug} name={circuit.name} /><span>0{index + 1}</span><b>{circuit.country.toUpperCase()}</b></div><p>{circuit.character.toUpperCase()}</p><h2>{circuit.name}</h2><div className="card-specs"><span>{circuit.distance}</span><span>{circuit.turns}</span><span>{circuit.lapType}</span></div><a href={`/circuits/${circuit.slug}`}>OPEN CIRCUIT NOTE <span>↗</span></a></article>)}</div></section><section className="calendar-section"><div className="calendar-heading"><div><p className="section-kicker"><span>02</span> FULL SEASON</p><h2>2026 circuit<br /><em>calendar.</em></h2></div><div><b>23 ROUNDS</b><p>Current calendar checked 22 August 2026. Circuit names are for identification; no official circuit artwork or branding is reproduced.</p><a href={calendar2026SourceUrl} target="_blank" rel="noreferrer">VIEW OFFICIAL F1 CALENDAR ↗</a></div></div><div className="calendar-grid">{calendar2026.map((race) => <article key={race.round}><span>ROUND {String(race.round).padStart(2, "0")}</span><time>{race.date}</time><h3>{race.circuit}</h3><p>{race.country} <i>·</i> {race.city}</p>{race.note && <b>{race.note}</b>}</article>)}</div><p className="calendar-credit">Calendar data: <a href={calendar2026SourceUrl} target="_blank" rel="noreferrer">Formula 1 official schedule ↗</a>. The Sepang replacement venue for the Bahrain Grand Prix is credited to the <a href="https://www.formula1.com/en/latest/article/formula-1-and-fia-confirm-malaysia-will-join-2026-calendar-as-host-venue-for-bahrain-grand-prix.6lL7vjFEM2VVynRHvg1TCf" target="_blank" rel="noreferrer">Formula 1 and FIA announcement ↗</a>.</p></section></AtlasShell>;
}
